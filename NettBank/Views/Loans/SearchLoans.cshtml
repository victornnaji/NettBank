@model IEnumerable<NettBank.Models.LoanCompany>
@using System.Globalization;
@{
    ViewBag.Title = "SearchLoans";
}

@*<h2>@ViewBag.loanTitle</h2>*@


<section class="Search-result">
    <div class="container">
        <div class="form-refine-holder mt-6">
            hello!!!!
        </div>

        <div class="loan-result-holder mt-10">
            @if (!Model.Any())
            {
                <div class="empty-result">
                    <div class="empty-image-holder flex justify-center items-center">
                        <img src="~/Content/Images/no_result.svg" class="empty-image" />
                    </div>
                    <div class="empty-text-heading mt-3 text-center tracking-tight leading-10 font-semibold text-gray-700  sm:leading-none md:text-5xl">
                        No Result found
                    </div>
                    <p class="mt-1 text-sm leading-5 text-center text-gray-500">Please refine your search and try again!</p>
                </div>
            }
            else
            {

                <h2 class="text-3xl leading-9 font-extrabold text-gray-700">
                    Compare @(ViewBag.loanTitle + "s") from @ViewBag.Money.ToString("C", CultureInfo.CreateSpecificCulture("ig-NG"))
                </h2>


                <table class="rwd-table mt-3">
                    <tr>
                        <th>Company</th>
                        <th>Interest Rate</th>
                        <th>Commison Rate</th>
                        <th>Monthly Payment</th>
                        <th>Loan Term</th>
                        <th>Ratings</th>
                        <th>Link</th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td data-th="Company">@Html.DisplayFor(modelItem => item.Name)</td>
                            <td data-th="Interest Rate">@Html.DisplayFor(modelItem => item.InterestRate)%</td>
                            <td data-th="Commison Rate">@Html.DisplayFor(modelItem => item.ComparisonRate)</td>
                            <td data-th="Monthly Payment">$460,935,665</td>
                            <td data-th="Loan Term">@Html.DisplayFor(modelItem => item.MaxDuration) yrs</td>
                            <td data-th="Ratings">
                                <div class="rating">
                                    @item.Rating
                                </div>
                            </td>
                            <td data-th="Link">Go To</td>
                        </tr>
                    }
                </table>
            }
        </div>
    </div>
</section>
@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InterestRate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ComparisonRate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RepaymentFrequency)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaxDuration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaxAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MinAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImagePath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Catch)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InterestRate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ComparisonRate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RepaymentFrequency)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaxDuration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaxAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MinAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImagePath)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Catch)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>*@
@section scripts{
    <script type="text/javascript">
       $(document).ready(function () {
           $('.rating').each(function () {
               var thz = $(this);
               const starTotal = 5;
               let average = thz.text();

                const starPercentage = (average / starTotal) * 100;
                const starPercentageRounded = `${((starPercentage / 10) * 10)}%`;
                thz.html(`<div class="stars-outer">
                            <div class="stars-inner" style="width:${starPercentageRounded}"></div>
                          </div>`);
           });
       })
    </script>
}